import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'josh_is_inLoveWithAtilla',
  webDir: 'www'
};

export default config;
